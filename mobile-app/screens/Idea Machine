import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  ScrollView,
  ActivityIndicator,
} from "react-native";

import OceanAIIdeaMachineSmart from "../components/OceanAIIdeaMachineSmart";

export default function IdeaMachineScreen() {
  const [mode, setMode] = useState("public");

  return (
    <ScrollView style={styles.container}>
      {/* عنوان صفحه */}
      <Text style={styles.title}>OceanAI Idea Machine</Text>
      <Text style={styles.subtitle}>
        ماشین تولید ایده برای اتوماسیون، فریلنسری و کسب درآمد.
      </Text>

      {/* توضیح حالت‌ها */}
      <View style={styles.modeBox}>
        <Text style={styles.modeTitle}>حالت تولید ایده:</Text>

        <View style={styles.modeRow}>
          <Text
            style={[
              styles.modeOption,
              mode === "public" && styles.modeActive,
            ]}
            onPress={() => setMode("public")}
          >
            عمومی
          </Text>

          <Text
            style={[
              styles.modeOption,
              mode === "personal" && styles.modeActive,
            ]}
            onPress={() => setMode("personal")}
          >
            شخصی
          </Text>
        </View>
      </View>

      {/* کامپوننت اصلی تولید ایده */}
      <View style={styles.section}>
        <OceanAIIdeaMachineSmart mode={mode} />
      </View>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "#050814", padding: 16 },
  title: { fontSize: 24, color: "#3ED9C7", fontWeight: "700" },
  subtitle: { color: "#8A8F9C", marginBottom: 20, fontSize: 13 },

  modeBox: {
    backgroundColor: "#0F111F",
    padding: 12,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: "#23262E",
    marginBottom: 20,
  },
  modeTitle: { color: "#F5A623", fontSize: 14, marginBottom: 10 },
  modeRow: { flexDirection: "row", gap: 12 },
  modeOption: {
    color: "#8A8F9C",
    fontSize: 14,
    paddingVertical: 6,
    paddingHorizontal: 12,
    borderRadius: 8,
    backgroundColor: "#23262E",
  },
  modeActive: {
    backgroundColor: "#3ED9C7",
    color: "#050814",
    fontWeight: "700",
  },

  section: { marginTop: 10 },
});
